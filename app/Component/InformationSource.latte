{$informationSource->author() ?: 'Autor není uveden'}
<cite>
    {$informationSource->place() ?: 'Místo zdroje není uvedeno'}
    {$informationSource->year() ? 'z roku ' . $informationSource->year() : ''}
</cite>
{if $user->isAllowed('Zdroj', 'upravit')}
    <a href="{plink Zdroj:Upravit $informationSource->id(), backlink => $backlink}"
       title="Upravit zdroj"><span class="glyphicon glyphicon-pencil"
                                   aria-hidden="true"></span></a>
{/if}