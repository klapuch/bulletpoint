{$author ?: 'Autor není uveden'}
<cite>
    {$place ?: 'Místo zdroje není uvedeno'}
    {$year ? 'z roku ' . $year : ''}
</cite>
{if $user->isAllowed('Zdroj', 'upravit')}
    <a href="{plink Zdroj:Upravit $id, backlink => $backlink}"
       title="Upravit zdroj"><span class="glyphicon glyphicon-pencil"
                                   aria-hidden="true"></span></a>
{/if}