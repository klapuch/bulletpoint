{block content}
{define description}Výsledky hledání pro klíčové slovo {$keyword} {/define}
<div class="row">
	<h1>{block title}Výsledky pro {$keyword}...{/block}</h1>
	<p>Nalezeno {$count} výsledků</p>
	<hr>
</div>
{if $count}
	<div n:foreach="$matches as $match" class="row">
		<a n:href="Dokument: $match['slug']"><h2>{$match[title]}</h2></a>
		<p>{$match[description]}</p>
		<a n:href="Dokument: $match['slug']">Více...</a>
		<hr>
	</div>
{/if}